// Live-tunable runtime settings, shared across player movement, the console,
// options window, and the renderer's lighting/time uniforms.
export const settings = {
  lightMode: 1,
  gravityAcceleration: -10.0,
  jumpSpeed: 4.5,
  walkSpeed: 4.2,
  editReachMeters: 6,
  carveRadius: 7,
  placeRadius: 4,
  waterFlowEnabled: true,
  fovDegrees: 70,
  timeOfDay: 12
};
